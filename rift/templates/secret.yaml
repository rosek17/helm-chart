{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helper.fullname" . }}-shared
  labels:
    {{- include "helper.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
