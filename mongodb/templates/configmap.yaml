apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mongodb.fullname" . }}-script
  labels:
    {{- include "helper.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "files/start-mongodb.sh").AsConfig | indent 2 }}
